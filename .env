# ─── Backend URLs ─────────────────────────────────────────────────────────────
# WebSocket endpoint for real-time chat (FastAPI)
VITE_BACKEND_WS_URL=ws://127.0.0.1:8000/ws

# HTTP base URL for REST calls (FastAPI)
VITE_BACKEND_HTTP_URL=http://127.0.0.1:8000
